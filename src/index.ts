import 'source-map-support/register';

export function pow1(x: number, y: number) {
    return x ** y;
}
export function pow2(x: number, y: number) {
    return x ** y;
}
export function someUnusedFunction(str: string) {
    console.log('[123] ' + str);
}
